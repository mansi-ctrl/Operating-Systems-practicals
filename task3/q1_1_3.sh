grep -n -E "\bm" *.*
#-E Interpret PATTERNS as extended regular expressions 
#with line number
# it's matching each and every characher of a line
#grep "^m" *.* 
#it's matching first character of a line
#-i for ignore case 
#grep -i "^m" *.*
